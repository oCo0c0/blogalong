// plugin
export enum pluginLabels {
  NAME = "plugin.blog.run/plugin-name",
}

// role
export enum roleLabels {
  TEMPLATE = "blog.run/role-template",
  HIDDEN = "blog.run/hidden",
  SYSTEM_RESERVED = "rbac.authorization.blog.run/system-reserved",
}

// post
export enum postLabels {
  DELETED = "content.blog.run/deleted",
  PUBLISHED = "content.blog.run/published",
  OWNER = "content.blog.run/owner",
  VISIBLE = "content.blog.run/visible",
  PHASE = "content.blog.run/phase",
}

// singlePage
export enum singlePageLabels {
  DELETED = "content.blog.run/deleted",
  PUBLISHED = "content.blog.run/published",
  OWNER = "content.blog.run/owner",
  VISIBLE = "content.blog.run/visible",
  PHASE = "content.blog.run/phase",
}
